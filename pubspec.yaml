name: more_less_text
description: A Flutter package for both android and iOS which provides collapsing and expanding texts
version: 0.0.1
homepage: https://github.com/Umidjon-18/more_less_text

environment:
  sdk: '>=3.0.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  expandable: ^5.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
